spring.application.name=WeeklyQuicklyLight

# DataSource configuration for MySQL database weeklyquicklylight
spring.datasource.url=jdbc:mysql://localhost:3306/weeklyquicklylight
spring.datasource.username=weeklyquickly
spring.datasource.password=weeklyquickly

# TODO: Change to Flyway for production
# Flyway configuration after Tables are settled
#spring.flyway.baseline-on-migrate=true
#spring.flyway.locations=classpath:db/migration
#spring.flyway.enabled=true

# TODO: Change to after Tables are settled
# Hibernate configuration
spring.jpa.hibernate.ddl-auto=create



# Logging configuration
logging.level.root=WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN

# switch off the banner
spring.main.banner-mode=off
